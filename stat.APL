max ←{⍵[⍴⍵]}
mu ←{+/⍵÷⍴⍵}
mean_dev ←{(+/|x-(mu ⍵))÷⍴⍵}
N ←{⍴⍵}
median ←{0.5×(+/⍵[(⌈(N ⍵)÷2),1+⌊(N ⍵)÷2])}
min← {⍵[⍋⍵][1]}
mu_g ← {(×/⍵)*(÷⍴⍵)}
mu_h ←{⍴⍵÷(+/÷⍵)}
sigma2 ←{(+/((⍵-(mu ⍵))*2))÷(⍴⍵-1)}
sigma ←{sigma2 ⍵*0.5}
cov← {((⍵+.×⍺)-((+/⍵)×(+/⍺))÷(⍴,⍵))÷(⍴,⍵)}
moments ← {(+/(⍵-(mu ⍵)*⍺))÷((⍴,⍵)-1)} 
poisson←{(*-⍵)×(⍵*X)÷!X←(0,⍳⍺)}
poisson_cumulative ← {+\⍺ poisson ⍵}
correlate ← {(+/((⍵-(mu ⍵))×(⍺-(mu ⍺)))) ÷ ((+/(⍵-(mu ⍵))*2) × (+/(⍺-(mu ⍺))*2))*0.5}
